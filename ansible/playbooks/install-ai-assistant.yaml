---
- hosts: localhost
  gather_facts: false
  vars_files:
    - ../vars/demo.yaml

  roles:
    - role: gitops-deploy-kiali
    - role: gitops-deploy-kubernetes-mcp
    - role: gitops-deploy-openshift-lightspeed

